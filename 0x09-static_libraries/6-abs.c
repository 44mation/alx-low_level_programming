#include "main.h"

/**
 * _abs - main code
 *
 * @n: the input
 * Description: return the absolute of a number
 *
 * Return: return the abs
*/

int _abs(int n)
{
	if (n < 0)
		n = (-1) * n;
	return (n);
}
